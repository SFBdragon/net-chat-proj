[project]
name = "net-chat-proj"
version = "0.1.0"
description = "A messanging app client and server for CSC3002F"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
    "pydantic>=2.12.5",
    "pysqlite3>=0.6.0",
    "textual>=8.0.2",
]
